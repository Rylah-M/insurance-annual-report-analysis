# 上市财险公司年报智能分析 Agent 项目汇报说明

## 一、项目概述

本项目名称为「上市财险公司年报智能分析 Agent」，是一套面向保险行业年报的智能化数据分析与报告生成系统。项目以「大语言模型（LLM）+ 数据工程 + Web 应用」为核心技术路线，实现了从「年报 PDF 上传」到「指标自动提取」「口径标准化」「横向对标分析」「经营分析报告生成」「智能问答」的完整业务闭环。

系统以保险公司年度报告为数据源，通过 PDF 解析、文本切片、指标召回、候选排序与 LLM 结构化抽取等环节，将非结构化的年报文本转化为结构化的保险经营指标数据库；在此基础上，进一步完成统计口径标准化、指标元数据治理、公司横向对标底稿生成、深度经营分析报告（Markdown / PDF）自动产出，并通过浏览器端 Web 系统对外提供服务，支持 Docker 一键部署。

## 二、项目背景

### 2.1 行业痛点

保险行业年报信息密度高、专业性强，主要存在以下痛点：

1. **人工提取效率低**：一份上市保险公司年报动辄数百页，关键经营指标（保费收入、综合成本率、偿付能力充足率等）分散在不同章节，人工阅读、摘录、核对耗时费力且容易遗漏。
2. **统计口径不统一**：不同公司年报的披露方式差异较大，同一指标可能是「集团合并口径」「财险单体口径」或「特殊财险口径」，直接横向比较会产生严重失真。
3. **分析报告产出周期长**：从数据整理到形成管理层可阅读的经营分析报告，通常依赖研究员手工制作 Excel 底稿并撰写文字，周期长、主观性强。
4. **数据可复用性差**：历史年报数据以文档形式散落存放，缺乏结构化数据库支撑，难以进行跨公司、跨年度的纵向与横向分析。

### 2.2 项目缘起

项目团队此前已完成多家上市公司年报的指标提取与人工核验，沉淀了 `database.csv` 等数据资产，并分别建设了 PDF 解析 Agent 与指标提取 Agent。本项目在此基础上，将实验性质的 Agent 代码升级为「数据库 → API → 网页展示 → 分析报告」的完整产品化系统，使研究成果从"脚本型 Agent"真正变为"可展示、可部署、可复用的软件产品"。

## 三、项目目标与定位

### 3.1 总体目标

打造一个**可公网访问、端到端自动化**的上市财险公司年报智能分析平台，最终用户只需打开浏览器，即可完成：

```
上传年报 → 自动解析 → 自动提取指标 → 数据入库 → 横向分析 → 生成分析报告 → 业务问答
```

### 3.2 分阶段目标

项目按十个阶段递进推进，遵循「先把真实数据分析闭环和 Web 架构做稳，再逐步接入已有 Agent」的原则：

| 阶段 | 主题 | 核心目标 |
| --- | --- | --- |
| 第一阶段 | 数据分析模块与 Web 架构设计 | 建设可复用的数据分析模块与可公网部署的前后端分离架构 |
| 第二阶段 | 数据分析 API 开发 | 基于 `database_result.csv` 提供公司、指标、比较、画像、图表等 REST API |
| 第三阶段 | 前端 Web 展示系统 | 开发 Dashboard、数据分析、报告展示页面 |
| 第四阶段 | 前后端联调与数据分析页面 | 打通"数据库 → FastAPI → 前端 → 浏览器"完整链路 |
| 第五阶段 | 智能分析 Agent 能力完善与部署 | 自动报告 Agent、智能问答 Agent、SQLite 指标库、Docker 部署 |
| 第六阶段 | 解析与指标提取 Agent 网页化接入 | 实现"上传 PDF → 自动解析 → 自动提取 → 展示"闭环 |
| 第七阶段 | 经营分析 Agent V2 | 从"报数"升级为"深度经营分析"，输出带证据链的 Markdown/PDF 报告 |
| 第八阶段 | business_scope 口径标准化 | 建立统计口径分类，解决指标不可比问题 |
| 第九阶段 | 指标元数据建设 | 以 `indicator_metadata.xlsx` 配置驱动分析规则 |
| 第十阶段 | 横向对标分析底稿自动生成 | 自动生成保险公司横向对标 Excel 分析底稿 |

### 3.3 项目定位

这是一个**两人小型 Agent 项目**，整体遵循"不扩大指标体系、不推翻已有算法、不破坏已有 Agent、不虚构数据、不过度工程化"的开发原则，优先保证**完整、真实、可运行、可展示**。

## 四、项目内容与系统架构

### 4.1 核心业务链路

```
年报 PDF
   ↓
MinerU 解析 → Markdown
   ↓
Markdown Splitter 切块 → chunks.json
   ↓
指标关键词召回 → 候选增强 → 相关性排序
   ↓
LLM 指标结构化抽取
   ↓
人工核验 → database_result.csv → SQLite
   ↓
business_scope_type 口径标准化 → indicator_metadata 元数据配置
   ↓
横向对标底稿（standardexcel.xlsx） / 深度经营分析报告（MD/PDF）/ 智能问答
```

### 4.2 技术架构

采用前后端分离架构：

```
浏览器
  ↓
React + Vite + TypeScript + ECharts（前端）
  ↓ HTTP / REST API
FastAPI（后端）
  ↓
pandas 数据分析层 / Agent 服务层
  ↓
database_result.csv + SQLite（company_info / indicator_data / report_info）
```

- **前端**：React + Vite + TypeScript + ECharts + Ant Design，页面包括首页 Dashboard、数据分析页、业务分析报告页、年报上传页、指标提取审核页、智能问答页、模型设置页等；所有业务数据均通过 FastAPI 动态获取，前端不写死业务数据。
- **后端**：Python FastAPI，提供公司/指标/年份查询、单指标横向比较、公司画像、描述性统计、图表数据、报告生成下载、智能问答、PDF 上传解析、指标提取任务管理等接口，并提供 Swagger 文档。
- **数据层**：`database_result.csv` 为事实源，首次启动自动同步为 SQLite 结构化数据库，初始化失败时自动回退 CSV，保证既有接口兼容。
- **Agent 层**：解析 Agent（MinerU + Markdown Splitter）、指标提取 Agent（召回→增强→排序→LLM 提取）、自动报告 Agent、智能问答 Agent、经营分析 Agent V2、标准底稿生成 Agent 等。
- **部署层**：Dockerfile + docker-compose.yml，挂载数据、报告与 Agent 目录，重启后数据不丢失，支持 `http://服务器地址:8000` 访问。

## 五、项目解决的问题

### 5.1 解决"年报指标人工提取"问题

通过「PDF 解析 → 文本切片 → 指标召回 → 候选排序 → LLM 抽取 → 人工核验」的自动化链路，将 LLM 对保险年报文本的理解能力转化为结构化指标数据。指标字典（indicator_dictionary.xlsx）提供了指标编号、别名、关键词、定义、单位、计算规则、来源优先级等业务知识，保证抽取的专业性；人工核验环节兜底数据质量。

### 5.2 解决"统计口径不可比"问题

针对不同公司披露口径不一致的问题，第八阶段在不修改原始 `business_scope` 字段的前提下，新增标准化字段 `business_scope_type`（枚举：集团口径 / 财险口径 / 特殊财险口径），并在审核页面提供人工确认下拉框。分析时仅保留财险口径与特殊财险口径、排除集团口径指标，从数据治理层面保证了公司间指标的可比性，这是保险年报分析中最核心的"统计口径一致性"问题。

### 5.3 解决"指标分析规则写死"问题

第九阶段建设 `indicator_metadata.xlsx` 指标元数据表，以配置驱动方式定义哪些指标参与比较、指标类别、标准单位、比较方向（越高越好/越低越好）、是否参与横向/纵向分析等规则，使分析模块从"写死指标"升级为"配置驱动"，为后续 Excel 生成、图表展示与报告生成提供了统一规则中心。

### 5.4 解决"对标底稿手工制作"问题

第十阶段实现横向对标分析底稿自动生成：读取数据库、指标元数据与指标字典三个文件，经口径过滤、指标匹配、数据透视，自动生成包含盈利能力、业务规模、经营效率、偿付能力对比及指标明细追溯 Sheet 的 `standardexcel.xlsx`，可直接用于保险行业研究。

### 5.5 解决"有数据无分析"问题

第七阶段经营分析 Agent V2 是项目分析能力的核心升级：引入独立 Analysis Engine（趋势、横向对标、结构、增长贡献、盈利质量、规模×盈利矩阵、指标联动、风险信号、阳光专项），要求所有重要指标必须经过 **Fact → Comparison → Interpretation → Insight** 四层信息结构，每个结论必须具有可追溯的数据证据链与置信度，并通过"So What Test"质量审稿（Critic Agent），从机制上避免"把数字罗列当成分析"，最终产出《2025年上市公司产险经营分析》Markdown 与 PDF 正式报告。

### 5.6 解决"数据不可达、不可复用"问题

通过 Web 化与 Docker 部署，用户无需安装 Python、Node.js 等环境即可通过浏览器使用系统；结构化指标库（CSV + SQLite）支撑跨公司、跨年度的横向比较与纵向趋势分析；智能问答 Agent 通过"数据库检索 + 年报原文知识库 + LLM 生成"的方式，回答业务问题并附数据来源引用，实现知识的可查询、可追溯。

## 六、项目成果

### 6.1 系统能力

- 完整的 Web 端"上传年报 → 自动解析 → 自动指标提取 → 数据分析展示"闭环；
- 自动经营分析报告（JSON / Markdown / HTML / PDF 多格式输出，含 SVG 图表）；
- 智能问答（数据库检索 + LLM，附来源引用，接口不可用自动回退模板回答）；
- 指标口径标准化与审核功能（business_scope_type）；
- 配置驱动的指标元数据体系（indicator_metadata.xlsx）；
- 横向对标分析底稿自动生成（standardexcel.xlsx）；
- 保险上市公司经营分析 Agent V2（输出带证据链与置信度的深度分析报告）。

### 6.2 数据资产

当前指标数据库画像：6 家上市公司（中国人保、中国太保、中国太平、中国平安、中国阳光、众安在线），覆盖 2024—2025 年（并持续扩展），指标涵盖盈利、规模、偿付能力、资产负债等财险经营指标体系，记录无重复、字段含来源文本与置信度。

### 6.3 报告产出

已产出《2025年上市公司产险经营分析》Markdown 与 PDF 报告，覆盖行业趋势、公司对标、阳光定位、增长贡献、承保盈利、风险信号等章节，例如：2025 年上市公司产险原保险保费收入合计 12,694 亿元、同比增长 3.7%，上市公司平均综合成本率 97.8%，阳光综合成本率 102.12% 且同比上升 2.4pt 等结论均附证据链。

## 七、项目价值与意义

1. **对保险研究**：把"AI 从年报中找数字"升级为"AI 建立可比较、可分析的保险经营指标数据库"，显著提升行业研究的效率与可比性。
2. **对经营分析**：实现了从"数据报告"到"经营分析报告"的跃迁，报告可直接回答"行业发生了什么、谁领先、阳光处于什么位置、为什么变化、哪些业务是机会/风险"等管理层关注的问题。
3. **对团队成长**：项目完整经历了数据工程、LLM Agent、前后端开发、系统部署的全流程实践，达到了毕业设计/科研展示/实习项目级别的完成度。
4. **对产品演进**：以配置驱动、模块解耦的方式设计（数据处理 Agent、Analysis Engine、叙事 Agent、Critic Agent 分离），具备良好的可扩展性，未来可平滑接入更多数据源、更多分析模块与更多展示形态。

## 八、后续规划与展望

1. **数据扩展**：持续补充更多公司、更多年度与更多指标，扩大横向对标样本；
2. **Web 集成**：将标准底稿生成 Agent 接入 Web 页面，提供"一键生成对标分析报告/Excel 下载"按钮；
3. **分析深化**：完善指标联动与风险预警能力，增加多年度趋势建模与预测；
4. **工程化提升**：接入正式数据库服务、对象存储与 CI/CD，提升系统稳定性与安全性；
5. **知识沉淀**：进一步扩充指标字典与元数据规则，降低对人工核验的依赖，提升端到端自动化程度。

## 九、总结

本项目以"上市财险公司年报智能分析"为业务场景，完成了从年报解析、指标提取、口径标准化、元数据治理、对标底稿生成到深度经营分析报告与智能问答的完整智能化链路，形成了一套可部署、可复用、可展示的 Agent 产品。项目不仅解决了年报数据处理与分析效率的行业痛点，更在统计口径一致性、分析结论可追溯性等专业问题上给出了工程化方案，是 LLM 技术应用于垂直金融数据领域的一次完整实践。
